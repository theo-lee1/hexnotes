---
interface Props {
	title: string;
	description: string;
	kicker?: string;
}

const { title, description, kicker } = Astro.props;
---

<header class="page-heading">
	{kicker && <p class="eyebrow">{kicker}</p>}
	<h1>{title}</h1>
	<p>{description}</p>
</header>
